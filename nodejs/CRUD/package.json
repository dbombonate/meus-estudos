{
  "name": "crud",
  "version": "1.0.0",
  "description": "CRUD para inclusão de clientes e pedidos. Base SQLITE.",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --inspect server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Bombonate",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
